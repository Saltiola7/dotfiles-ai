# dotfiles-ai

Portable macOS configuration for DBSCTR, OpenCode, and Herdr, managed by an
independent chezmoi source repository.

Implementation is tracked in `docs/specs/dotfiles_ai_distribution/`.
