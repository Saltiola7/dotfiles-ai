# DAI-034: Add Portable Remote Workspace Client

## Status

Implemented; draft delivery pending.

## Intent

Provide an opt-in local client profile for a repository-managed remote workspace
without committing endpoint identity, account selection, credentials, or network
policy.

## Acceptance

- Machine-local TOML owns the repository path, project, zone, instance, optional
  account, and optional alternate host values.
- Chezmoi renders only non-secret shell environment values when enabled.
- macOS package setup provides mise and Google Cloud CLI alongside OpenCode.
- A local doctor fails before use when required tools or coordinates are absent.
- A dedicated local command forwards to existing repository-owned mise tasks.
- Focused portable-distribution tests and shell syntax checks pass.

## Visual Evidence

- Visual Evidence Plan: `not_applicable`; this slice adds no visual interface.
- Text Equivalent: local TOML enables private environment rendering, then the
  doctor reports readiness or one actionable missing prerequisite.
- Quantitative: focused tests assert all five rendered values, the managed doctor,
  required packages, prerequisite success, and public-tree identity hygiene.

## Completion Evidence

- `559 passed, 1 skipped` in the full repository suite.
- Ruff, shell syntax, focused rendering checks, and Git diff hygiene passed.
- Gate Commit: `73f8ff1ef29532676edd7c5687025133cfcc3b7b`.
- Live installation and network enrollment are not performed by this draft-only
  configuration cycle.
