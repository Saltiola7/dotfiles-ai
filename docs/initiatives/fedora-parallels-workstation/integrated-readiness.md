# Integrated Workstation Readiness

Discovery marks the workstation delivered only after every Build slice is
delivered and live evidence proves the VM topology, storage and share modes,
Parallels Tools, personal/AI target separation, retained data state, updates,
Atuin synchronization, tailnet SSH, and both AI runtime qualifications.

Missing external storage, low-capacity refusal, retained-kernel recovery,
incompatible-state retention, cache reset, and source rollback must be exercised
without deleting private state or changing host project data outside a disposable
probe file.
