---
description: Run the DBSCTR software lifecycle
---

Use the skill tool to load `dbsctr`, then execute it against the arguments below.
Do not answer from memory without loading the skill.

$ARGUMENTS
