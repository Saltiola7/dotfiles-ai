---
description: Generate a private factual Sprint Review report
---

Use the skill tool to load `pm-kernel` in Sprint Review mode. Require an explicit
bounded Jira selection or approved JQL, include every selected Done item exactly
once, never invent goals or outcomes, and save beneath the configured Git-ignored
`data/interim/reports/report_type=sprint_review/snapshot_date=<date>/` path.

$ARGUMENTS
