---
description: Generate a private factual Sprint Review report
---

Use the skill tool to load `pm-kernel` in Sprint Review mode. Preview one exact
project-scoped, AND-only JQL expression and require confirmation of its SHA-256
before the bounded ACLI read. Include every returned Done item exactly once,
never invent goals or outcomes, and save beneath the configured Git-ignored
`data/interim/reports/report_type=sprint_review/snapshot_date=<date>/` path with
the JQL digest, selected keys, and source snapshot digest.

$ARGUMENTS
