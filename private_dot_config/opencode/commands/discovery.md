---
description: Discover a DBSCTR-ready bounded context
---

Use the skill tool to load `discovery`, then execute it against the arguments
below. Do not answer from memory without loading the skill.

$ARGUMENTS
