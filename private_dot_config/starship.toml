# Keep the prompt visually aligned with the personal dotfiles.
format = """
$all\
$env_var_SHELL_LABEL\
$line_break\
$character"""

[nodejs]
disabled = true

[shell]
disabled = true

[env_var.SHELL_LABEL]
format = '[🐚 $env_value]($style) '
style = 'white bold'
